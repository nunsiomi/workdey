{
  "title": "WorkDey Career Agent Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "fullName": {
      "title": "Full Name",
      "type": "string",
      "description": "Your full name for personalizing outreach materials and the dashboard.",
      "editor": "textfield",
      "default": "Olamide Lawal",
      "prefill": "Olamide Lawal"
    },
    "email": {
      "title": "Email Address (Optional)",
      "type": "string",
      "description": "Where to send your application pack and match report. Leave empty to skip email.",
      "editor": "textfield"
    },
    "targetRoles": {
      "title": "Target Roles / Keywords",
      "type": "array",
      "description": "Job titles or target domains (e.g., Data Scientist, UI/UX Designer, Product Designer).",
      "editor": "stringList",
      "minItems": 1,
      "default": ["Data Scientist", "Machine Learning Engineer"],
      "prefill": ["Data Scientist", "Machine Learning Engineer"]
    },
    "preferredLocations": {
      "title": "Preferred Work Mode / Location",
      "type": "array",
      "description": "e.g., Remote, Lagos, Nigeria, Worldwide, Hybrid.",
      "editor": "stringList",
      "default": ["Remote", "Nigeria", "Worldwide"],
      "prefill": ["Remote", "Nigeria", "Worldwide"]
    },
    "cvText": {
      "title": "CV / Resume Text",
      "type": "string",
      "description": "Paste your full CV or career summary text.",
      "editor": "textarea",
      "minLength": 50,
      "default": "Machine learning and software engineer experienced in Python, PyTorch, predictive modeling, data pipelines, and embedded IoT architectures.",
      "prefill": "Machine learning and software engineer experienced in Python, PyTorch, predictive modeling, data pipelines, and embedded IoT architectures."
    },
    "minScore": {
      "title": "Minimum Compatibility Score (%)",
      "type": "integer",
      "description": "Only jobs matching at or above this score threshold will be included.",
      "minimum": 1,
      "maximum": 100,
      "default": 60,
      "prefill": 50
    },
    "maxPacks": {
      "title": "Maximum Application Packs to Draft",
      "type": "integer",
      "description": "How many tailored cover letters and recruiter DMs to generate for top matches.",
      "minimum": 1,
      "maximum": 5,
      "default": 2,
      "prefill": 1
    },
    "resetMemory": {
      "title": "Reset Memory (Ignore Previously Seen Jobs)",
      "type": "boolean",
      "description": "Enable this to re-evaluate and clear previously saved job history. Turn off for scheduled runs so you only get new jobs.",
      "default": false,
      "prefill": true
    },
    "maxJobAgeDays": {
      "title": "Maximum Job Age (Days)",
      "type": "integer",
      "description": "Ignore listings older than this many days. Listings with an unknown post date are kept.",
      "minimum": 1,
      "maximum": 365,
      "default": 30
    },
    "includeSocialSignals": {
      "title": "Scan Verified X Accounts for Hiring Signals",
      "type": "boolean",
      "description": "Looks at public posts from verified X (Twitter) accounts that say they are hiring or need a service you offer. Needs an X API token below; skipped otherwise. Instagram is not scanned because it has no public search that respects its terms. Social leads are marked 'verify manually'.",
      "default": true
    },
    "xBearerToken": {
      "title": "X (Twitter) API Bearer Token (Optional)",
      "type": "string",
      "description": "Your own X API v2 bearer token, used read-only to search public posts from verified accounts. Stored as a secret. Leave empty to skip social signals.",
      "editor": "textfield",
      "isSecret": true
    }
  },
  "required": ["fullName", "targetRoles", "cvText"]
}